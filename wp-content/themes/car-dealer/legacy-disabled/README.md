# Legacy disabled assets

These assets are preserved for reference but are no longer loaded by the active
theme bootstrap. The current frontend loads:

- `style.css`
- `assets/css/main.css`
- `assets/js/main.js`

Move styles or scripts back only after checking for duplicate selectors and
missing dependencies.
